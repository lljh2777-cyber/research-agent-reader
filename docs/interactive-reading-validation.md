# 交互深读首版验收记录

日期：2026-09-05。环境：Windows、Obsidian Desktop 1.13.7。功能与操作说明见 [交互深读](interactive-reading.md)。

## 分阶段提交

| 阶段 | 提交 | 内容 |
| --- | --- | --- |
| 1 | `7356b0a` | 会话、分支快照和独立存储 |
| 2 | `f8fc1d9` | PDF 与已验证 MinerU 证据 |
| 3 | `fb86ccc` | 双模式界面和浮动对话 |
| 4 | `d940b4a` | 导读 SKILL 与两个模型后端 |
| 5 | `96b6365` | 支线记忆与划选追问 |
| 6 | `a388ddf` | 知识库补充与笔记导出 |
| 7 | `0b587bc` | 集成修复、回归、使用说明 |

各阶段均执行相关检查后提交并推送。阶段 3 起将构建产物交付测试库；替换前备份旧插件，设置与阅读会话独立保留。

## 自动化检查

- `pnpm typecheck`、`pnpm build`、`git diff --check` 通过。
- `pnpm test:reading` 的 8 组测试通过：会话、文档、布局、生成引擎、后端、支线、导出、集成。使用内存存储和模拟后端，不删除文件。
- 定向旧功能回归通过：知识库对话、Provider、批注、批注来源完整性、入库证据、安全 Markdown、MinerU 阅读器。
- `pnpm release:check` 的 28 项发布审计通过。导读 SKILL 格式校验通过。

检查了运行脚本的清理行为。完整旧测试入口存在递归清理路径，因此未运行 `pnpm test` / `pnpm verify`。旧阅读器测试中的可选私有样本未提供；作为补充，在测试库实际读取了 5 个已验证 MinerU 包。没有运行批量或递归删除。

## Obsidian 实际验证

| 场景 | 结果 |
| --- | --- |
| 原始 PDF × Direct API | 成功生成提纲及第一单元 |
| 原始 PDF × Codex CLI | 成功生成提纲及第一单元 |
| MinerU article.md × Direct API | 成功生成提纲及第一单元，包含实际图像输入 |
| MinerU article.md × Codex CLI | 成功生成提纲及第一单元 |
| 主线继续与问题支线 | 真实论文成功生成第二单元及概念追问；既有支线背景保持独立 |
| 双模式、节点跳转、草稿目标 | DOM 操作通过 |
| 支线续问、双击发送、划选子支线 | DOM 操作通过，父节点和引用一致 |
| 窗口固定、拖动、调整大小、收起 | DOM 操作和持久化通过 |
| 300 节点 | 全部显示，折叠后 291 个可见节点；关闭再打开恢复折叠及滚动位置；一次实测渲染约 118 ms |
| 插件重载 | 会话、节点、草稿与未发送引用恢复，未出现会话加载错误 |
| PDF 证据 | 读取原文片段并显示原始页面图像 |
| MinerU 原文跳转 | 图表显示模式下也能定位正文第 5 页 |
| 本地 Wiki 补充 | 实际读取论文及综合笔记；排除日志、批注与学习导出目录 |
| 学习笔记导出 | 实际新建含 2 个主线单元、1 条支线的笔记，并追加日志；来源使用非链接路径 |

真实模型使用测试库已有的 Direct API 配置（`qwen3.7-flash`）和 Codex CLI 配置。未新增凭据。测试记录保留在插件会话目录；示例会话有明确标记，真实学习记录不被写成正式 X-Ray 结论。

界面脚本为 `tests/obsidian-reading-scenario.cjs` 和 `tests/obsidian-reading-long.cjs`，需通过 Obsidian CLI 显式执行。原文阅读器的截图与跳转检查期间临时关闭 Electron 的后台节流，完成后恢复；新阅读模块的超时、草稿保存及 PDF 证据渲染另行修复了后台等待问题。

## 集成中修复的问题

- Obsidian 自定义视图默认禁止文字选择，导致划选追问无法触发。
- Direct API 的 20 秒默认超时不适合长文，阅读请求改用 120 秒并保留独立取消。
- 后台 PDF 图像渲染等待前台动画帧，停止无法及时取消；改用离屏渲染方式并传播取消信号。
- 主输入框草稿未按主线节点隔离；切换支线会影响主输入目标；滚动与未发送选区缺少恢复。
- 主线视图恢复时平滑滚动依赖动画帧；恢复位置改为立即设置。
- 长文目录截断可能遗漏后半篇证据；改为保留全部位置并压缩预览，超出目录容量明确拒绝。
- 已选图像超量、未加载或模型没有视觉能力时，禁止将文字解释记录为图像读取。
- 生成结束后再次核对原文指纹，避免请求期间文件变化仍写入完成结果。
- 存储失败恢复时保留可读历史；界面监听器异常不再误报已经完成的磁盘保存失败。
- Wiki 检索排除操作日志及自动学习记录，减少循环引用。
- 图表模式的原文阅读器跳转新增明确页码导航。

## 已记录的限制与后续验收

- 某些 MinerU 段落不能唯一匹配页码或跨越多页；显示章节、原文片段和字符范围，保留页码缺口。
- Codex CLI 正文在最终消息完成时显示；具备流式能力的 Direct API 支持逐步显示。生成阶段和停止状态均可见。
- 每轮最多 3 张图像，较大的多面板图需要分次提问。本文事实仍需读者对照证据判断。
- 这次验证覆盖程序流程和实际界面操作，不能代替用户对长时间阅读舒适度、讲解质量和布局偏好的验收。
- 首版未加入联网检索、Claude/OpenCode 阅读适配器或正式论文 `x-ray` 状态自动升级，符合约定范围。

## 界面改版验收（2026-09-07）

本轮重新设计交互深读的导航、排版、节点、引用、输入区和浮窗。仍使用 Obsidian DOM、内置图标与 SVG 连线，没有新增前端依赖。阅读模型、证据、存储与记忆规则沿用阶段 7。

- 类型检查、生产构建、9 组阅读测试（新增显示层引用测试）、7 组定向旧功能回归通过。发布审计与差异检查通过。
- 在 Obsidian 1.13.7 中查看了真实论文的深色、浅色、双模式、支线浮窗及 880 CSS 像素宽分栏。只临时切换主题和测试视图宽度，检查后恢复。
- 实际 DOM 回归覆盖双模式、节点跳转、目标与草稿、支线续问、重复发送、固定/拖动/调整大小/收起、划选子支线和保存。
- 新增显示层回归验证数字引用打开对应依据、包含数字引用的选区还原为原始 Markdown 偏移、未知引用保留、重复标题仅在显示时移除、正确聚焦引用窗口、键盘分栏、缩放及新建/导出/模型入口。
- 300 节点全量渲染，折叠后 291 个可见节点，关闭重开后恢复折叠与滚动；本机单次渲染约 191 ms，仅作为一次观测。
- 构建文件更新前备份旧插件；仅替换 `main.js`、`styles.css`、`manifest.json`，校验 SHA-256，保留设置、阅读会话与运行材料。验收脚本创建的示例会话保留且明确标记。

未重复调用真实模型；本轮聚焦显示和交互，两个后端及两类来源的真实生成沿用阶段 7 记录。未运行含递归清理的完整旧测试入口；定向 MinerU 回归缺少可选私有样本，未将其记为通过。没有批量或递归删除。

### 小窗空间与尺寸调整（2026-09-07）

- 将缩放扩展到四边和四角，右下角提供可聚焦的斜向箭头及方向键微调；增加最小尺寸、可见区域约束和指针取消时的监听清理。
- 压缩标题栏、正文内边距和输入区。相同的 520 × 560 小窗中，正文区域从约 60% 提升到约 78%；输入区从约 134 像素降至约 61 像素。长草稿自动增高至上限后内部滚动，清空后缩回。
- 类型检查、生产构建、9 组阅读测试与原有 Obsidian 双模式交互回归通过。新增小窗 DOM 验收覆盖八方向尺寸变化、对侧边界保持、最小/最大范围、按下不跳动、指针取消、键盘和焦点恢复、正文占比、发送按钮不被缩放柄覆盖、草稿增高与尺寸保存；重载插件后尺寸恢复。
- 旧插件先备份，复制三个构建文件并核对哈希；设置、已有会话和运行材料保留。未运行含递归清理的完整旧测试入口，本次也未调用模型。

### 会话管理完善（2026-09-07）

- 类型检查、生产构建、10 组阅读测试、知识库对话与 Provider 定向回归通过。新增会话目录测试覆盖旧数据识别、真实记录保护、分类/置顶/归档、独立存储与失败恢复、指纹变化、并发来源复用和显式新建。
- Obsidian 会话面板实测搜索、置顶、改名、归档与恢复通过；真实 MinerU 来源再次打开时复用已有会话且不产生新模型请求。默认面板只展示 4 个真实论文会话，同名来源分组；原有示例可在独立分页查看。
- 所有交互验收脚本改为显式创建 `test` 会话，新记录保存在 `reading-test-sessions/`。旧测试记录不移动、不删除，真实原文和历史回答保留。

### 追问目标与划选操作（2026-09-07）

- 类型检查、生产构建、10 组阅读测试通过；Obsidian 双模式与分支、引用与排版、小窗尺寸三个 DOM 回归通过。
- 实测单纯划选不打开小窗，点击追问才保存引用，跨数字引用的原始偏移保持正确。仅导图模式的主输入框收起再展开后草稿保留，小窗明确显示子支线或续问目标。
- 保留八方向缩放、正文占比、重复发送保护和草稿保存；复制入口已检查，实际剪贴板写入未在回归中执行，以免覆盖用户剪贴板。本轮未调用模型，未运行含递归清理的完整旧测试入口。
