# 主线导读质量

教学规则保持简短：一个中心问题、解释方法理由、区分事实与解释、保留指标与图表含义、连贯推进。正文结构、篇幅和例子由模型按内容决定，不要求固定章节或填满检查清单。

`skills/paper-guided-reading/SKILL.md` 随构建嵌入两个阅读后端。程序另行提供当前任务、输出协议及来源边界；编号、状态、存储和学习标记由插件管理。

## 0.37.0 的模块计划

尚无提纲和已完成主线的新会话，先根据全文证据目录及节选规划路线，再按当前单元选取并读取原文。规划覆盖全文位置，不代表已经逐项核验全文；纯扫描且不超过三张图像时，规划实际读取全部图像，超过容量或后端不支持图片时明确提示补足可读来源，不根据空白页码猜测路线。

每项只保存标题、中心问题、候选证据 ID。计划与现有 `outline` 一并原子保存到阅读会话，候选 ID 不计为实际引用。第一项作为全文总览，最后一项作为论证与缺口综合；单元标题、位置和结束判断由程序维护。规划失败不推进，讲解失败后重试和重载复用已保存路线。已有提纲或已有完成主线的旧会话不重新规划。

首次开始通常比原流程增加一次规划调用，之后仍是证据选择和讲解；长支线按原有规则摘要。规划用量单独显示，不增加每轮自评调用。经过验证的 Direct API 配置使用当前可用 ID 的原生 Schema，其他配置与 Codex CLI 保留本地校验。背景摘要维持原有机制，不新增复杂概念状态。

两种视图共享相同会话。阅读路线中展示中心问题，阅读助手可以取得下一单元的准确编号、标题和问题；支线取证与回答不接收最新主线模块，继续使用自身背景快照。讲解规则改善不等于已经证明模型答案质量提升，仍需按下面的场景对照原文评估。

正文完全缺少有效证据标记时，回答校验失败且不推进。用户重试该节点时，才附上缺失引用的反馈；不自动重试，不为模型补造引文。此检查能发现完全漏标，不能证明每项结论均已充分引用或事实正确。

## 质量对照

下面是人工评估场景，不是已经通过的模型实验。比较讲解内容，不按小标题数量评分；自动化检查负责引用、顺序及保存等可判定约束。

| 场景 | 观察重点 |
| --- | --- |
| 同时报告 Top-1、Top-k 与实验验证率 | 保留各自任务与分母，避免直接当作同一种准确率比较 |
| 工作流示意图与结果图相邻 | 区分设计说明与实验证据；读图说明有用的坐标轴、对照和不确定性 |
| 正文引用未提供的补充表 | 指出具体缺口，继续解释有证据的部分，不补造表格内容 |
| 下一模块沿用上一讲概念 | 简短衔接后回答新问题，避免换标题重复前文 |
| 从旧回答划选公式追问 | 聚焦选区与必要例子，保持支线背景及主线位置 |
| 最后一个综合单元 | 综合已讲的论点与证据，保留未解决缺口，不把讲完等同于掌握或 X-Ray |

后续仅针对反复出现且可复现的问题增加约束，避免将某篇论文的特殊情况写成所有论文的要求。

## 2026-09-08 验收记录

- SKILL 文件从 1,150 字符缩短至 532 字符（包含前置元数据，减少约 54%）。程序协议另行传入，此比例不是整次请求 token 或费用的降幅。
- 类型检查、生产构建、17 组阅读回归、4 组助手回归及 28 项发布审计通过。测试使用内存数据和模拟后端，无批量或递归清理；未运行包含旧清理逻辑的全量测试入口。
- 新增回归覆盖：路线保存与恢复、旧会话兼容、未知候选 ID、重复模块、来源变化、保存失败、取消、规划与选择复用、支线不接收最新主线模块、纯扫描页的图像条件，以及漏标引用后的显式重试。

真实模型使用同一篇 Novae 论文，测试会话保存到 `reading-test-sessions/`，不写正式论文笔记，也不进入默认阅读列表。

| 后端与来源 | 结果 | 模型调用 |
| --- | --- | --- |
| Direct API：qwen3.7-flash，已验证 article.md | 规划 8 个单元，生成总览和下一单元；第二单元复用原路线 | 首轮 3 次，下一轮 2 次 |
| Codex CLI：gpt-5.6-terra，原始 PDF | 规划 10 个单元，生成总览 | 首轮 3 次 |

Direct API 此次流式请求未报告实际 token，用量面板显示估算及未知状态。Codex CLI 报告输入 76,388、输出 2,165 token，含 CLI 附加上下文；不能与精简 SKILL 的字符数直接比较。本轮没有进行严格的旧规则/新规则对照实验。

实测暴露并修复了“只有证据清单、正文完全未标引用仍可通过”的校验缺口。更新后将三条真实回答回放给校验器，漏标的一条被拒绝，其余两条通过引用结构校验；显式重试的定向反馈和缓存复用由回归测试覆盖，没有自动追加付费修复请求。旧测试回答保持原样，便于追溯。

事实质量仍有明确限制：Direct 总览将 Xenium 写成 Illumina、MERSCOPE 写成 10x Genomics。论文 `Datasets used` 原文给出的对应厂商依次是 10x Genomics、Vizgen、Nanostring。未添加厂商专用规则或改写模型历史，此错误说明精简规则与有效引用均不能保证科学内容正确。本次验收不宣称回答事实质量全部通过。

Obsidian 内验证了 8 项路线与中心问题展示（无横向溢出）、仅导图模式下打开正文小窗、切回分栏、计划与回答重载恢复；操作前后模型调用数保持 5 次，仓储恢复无错误，控制台无报错。三份部署产物校验一致，设置与历史备份校验通过，所用 PDF 和 article.md 的哈希保持不变。
